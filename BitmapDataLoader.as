package {    import flash.display.Bitmap;    import flash.display.BitmapData;    import flash.display.Loader;    import flash.display.Sprite;    import flash.events.*;    import flash.geom.Point;    import flash.geom.Rectangle;    import flash.net.URLRequest;    public class BitmapDataLoader extends Sprite {				public var image:Bitmap;		public var fn:String;        public function BitmapDataLoader(_fn:String) {            configureAssets(_fn);        }        private function configureAssets(_fn:String):void {			this.fn = _fn;            var loader:Loader = new Loader();            loader.contentLoaderInfo.addEventListener(Event.COMPLETE, completeHandler);            loader.contentLoaderInfo.addEventListener(IOErrorEvent.IO_ERROR, ioErrorHandler);            var request:URLRequest = new URLRequest(this.fn);            loader.load(request);            addChild(loader);        }               private function completeHandler(event:Event):void {            var loader:Loader = Loader(event.target.loader);            image = Bitmap(loader.content);			//image.name = this.fn;			trace("INFO: load image "+fn+" Complete !!!");           	addChild(image);        }                private function ioErrorHandler(event:IOErrorEvent):void {            trace("Unable to load image: " + this.fn);        }    }}